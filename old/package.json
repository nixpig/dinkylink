{
  "name": "root",
  "private": true,
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "clean": "lerna run clean",
    "build": "lerna run build",
    "start": "lerna run start",
    "test": "lerna run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "lerna": "^7.2.0",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "typescript": "^5.2.2"
  }
}
